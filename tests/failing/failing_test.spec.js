// tests/failing
// failing_test.spec.js
import { expect } from "chai";

describe("Padající test", () => {
  it("1 rovná se 2", () => {
    expect(1).to.equal(2);
  });
});
/*

Cvičení (⌛10:00):
Vytvořte jednoduchou kalkulačku v nové složce: src/calculator
Calculator (class) bude obsahovat 4 metody, každá bude mít 2 parametry (příklad: add(a, b):
sčítání (add)
odčítání (subtract)
dělení (divide)
násobení (multiply)
Metody budou navracet (pomocí return) výsledek operace. 
Constructor není potřeba.

*/
